/**
 * dsh-thalamus client half: the notification/preview drawer.
 *
 * Placeholder for M3/M4: registers nothing yet. The host half (src/index.ts)
 * provides ctx.notifications; the drawer will register into shell.overlay.
 */

/** Client plugin body (empty until M3 UI). */
export function apply(): void {
  // M3: register the ThalamusDrawer into shell.overlay.
}
